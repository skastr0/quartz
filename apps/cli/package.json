{
  "name": "@skastr0/quartz-cli",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "quartz": "./dist/main.js"
  },
  "scripts": {
    "build": "bun build ./src/main.ts --outdir ./dist --target bun --format esm",
    "install:local": "bun run build && bun link"
  },
  "dependencies": {
    "@skastr0/quartz-core": "workspace:*"
  }
}
