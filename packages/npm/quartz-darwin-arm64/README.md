# @skastr0/quartz-darwin-arm64

Quartz standalone CLI binary for macOS arm64.

This is an optional platform package used by `@skastr0/quartz`. Install and run the user-facing CLI package instead:

```bash
npx -y @skastr0/quartz capabilities
```
