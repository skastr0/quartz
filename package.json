{
  "name": "type-level-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run apps/cli/src/main.ts",
    "build:cli": "bun scripts/build-cli.ts",
    "install:local": "bun scripts/install.ts",
    "cli:build": "bun run --filter @type-level-tools/cli build",
    "cli:install-local": "bun run --filter @type-level-tools/cli install:local",
    "build": "bun run --filter './apps/*' build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify": "bun run typecheck && bun run test && bun run build"
  },
  "dependencies": {
    "effect": "^3.21.0",
    "ts-morph": "^27.0.2"
  },
  "devDependencies": {
    "@effect/vitest": "^0.29.0",
    "@opencode-ai/plugin": "^1.4.0",
    "@types/bun": "^1.3.11",
    "typescript": "^6.0.3",
    "vitest": "^4.1.2"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.4.0"
  }
}
