{
  "name": "@skastr0/quartz-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/npm/*"
  ],
  "scripts": {
    "dev": "bun run apps/cli/src/main.ts",
    "build:cli": "bun scripts/build-cli.ts",
    "build:npm-cli": "bun scripts/build-npm-cli-packages.ts",
    "install:local": "bun scripts/install.ts",
    "core:build": "bun run --filter @skastr0/quartz-core build",
    "cli:build": "bun run --filter @skastr0/quartz-cli build",
    "cli:install-local": "bun run --filter @skastr0/quartz-cli install:local",
    "build": "bun run core:build && bun run --filter './apps/*' build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify": "bun run typecheck && bun run build && bun run test && bun run verify:package-boundaries",
    "verify:package-boundaries": "bun scripts/verify-package-boundaries.ts",
    "verify:docs-examples": "bun scripts/verify-doc-examples.ts",
    "verify:effect-rewrite": "bun scripts/verify-effect-rewrite.ts",
    "verify:external-matrix": "bun scripts/external-feature-matrix.ts",
    "verify:regression-guard": "bun scripts/verify-regression-guard.ts",
    "pack:dry-run": "bun run build && bun run build:npm-cli && npm pack --dry-run \"./packages/core\" && npm pack --dry-run \"./apps/opencode-plugin\" && npm pack --dry-run \"./packages/npm/quartz-darwin-arm64\" && npm pack --dry-run \"./packages/npm/quartz-darwin-x64\" && npm pack --dry-run \"./packages/npm/quartz-linux-arm64\" && npm pack --dry-run \"./packages/npm/quartz-linux-x64\" && npm pack --dry-run \"./packages/npm/quartz\"",
    "smoke:npm-cli": "bun run build:npm-cli && node packages/npm/quartz/bin/quartz.js capabilities",
    "release:check": "bun run verify && bun run pack:dry-run && bun run smoke:npm-cli && bun run verify:regression-guard && bun run verify:external-matrix && bun audit && publish-scan ."
  },
  "dependencies": {
    "effect": "^3.21.2",
    "ts-morph": "^27.0.2"
  },
  "devDependencies": {
    "@effect/vitest": "^0.29.0",
    "@opencode-ai/plugin": "^1.14.46",
    "@types/bun": "^1.3.11",
    "typescript": "^6.0.3",
    "vitest": "^4.1.2"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.4.0"
  },
  "overrides": {
    "brace-expansion": "5.0.6",
    "uuid": "13.0.2"
  },
  "license": "MIT",
  "description": "TypeScript code intelligence and transform search tooling.",
  "keywords": [
    "quartz",
    "typescript",
    "type-analysis",
    "code-intelligence",
    "static-analysis",
    "agent-tools"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/skastr0/quartz.git"
  },
  "homepage": "https://github.com/skastr0/quartz#readme",
  "bugs": {
    "url": "https://github.com/skastr0/quartz/issues"
  }
}
